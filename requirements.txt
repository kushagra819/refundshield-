# Runtime dependencies for the Vercel deployment (api/index.py).
# The backend's own dev/test extras live in backend/requirements.txt.
fastapi>=0.115
pydantic>=2.10
numpy>=2.0
pandas>=2.2
networkx>=3.3
